<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h1>Pokemon Seleccionado: <% data.forEach(item => { %> <%= item.pokename %> <% }) %> </h1>
    <table>
        <tr>
            <th id="idPoke">ID Pokemon</th>
            <th id="pokemon">Pokemon</th>
            <th id="hp">HP</th>
            <th id="atk">Ataque</th>
            <th id="def">Defensa</th>
            <th id="spatk">Ataque Especial</th>
            <th id="spdef">Defensa Especial</th>
            <th id="speed">Velocidad</th>
            <th id="dualtype">Tipo</th>
            <th id="opciones">Opciones</th>
        </tr>
        <tr>
            <% data.forEach(item => { %>
            <!-- Para cada Pokémon en los datos, comienza una nueva fila de la tabla -->
            <tr>
                <!-- Muestra cada propiedad del Pokémon en una celda de la tabla -->
                <td><%= item.IDpoke %></td>
                <td><%= item.pokename %></td>
                <td><%= item.HP %></td>
                <td><%= item.attack %></td>
                <td><%= item.defense %></td>
                <td><%= item.spattack %></td>
                <td><%= item.spdefense %></td>
                <td><%= item.speed %></td>
                <td><%= item.dualtype %></td>
                <!-- En la última celda, muestra los botones de opciones -->
                <td class="opciones">
                    <a href="/options/view/<%= item.IDpoke %>"><button type="button" disabled id="Ver" >Ver</button></a>
                    <a href="/options/edit/<%= item.IDpoke %>"><button id="Editar">Editar</button></a>
                    <a href="/options/delete/<%= item.IDpoke %>"><button id="Eliminar">Eliminar</button></a>
                </td>
            </tr>
            <% }) %>
        <!-- Termina la fila de la tabla -->
        </tr>
    </table>
</body>
</html>

<style>
    :root{
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }
    h1 {
        text-align: center;
    }
    table {
        margin: 0 auto;
        width: 80%;
        border-collapse: collapse;
    }
    th, td {
        border: 1px solid black;
        padding: 0.5em;
    }
    th {
        background-color: #f0f0f0;
    }
    tr:nth-child(even) {
        background-color: #f0f0f0;
    }

    #Ver:disabled {
        background-color: #1a521c;
        color: white;
        padding: 5px 20px;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 5px;

        
    }

    #Editar {
        background-color: #008CBA;
        color: white;
        padding: 5px 20px;
        border: none;
        cursor: pointer;
        border-radius: 10px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 5px;
    }

    #Eliminar {
        background-color: #f44336;
        color: white;
        padding: 5px 20px;
        border: none;
        cursor: pointer;
        border-radius: 10px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 16px;
        margin: 5px;
    }

